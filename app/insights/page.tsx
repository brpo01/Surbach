'use client'
import { useMemo, useState } from 'react'
import { Search } from 'lucide-react'
import { Header, Footer, Hero, SectionHeading, CTASection, InsightCard, insights } from '@/components/site'
export default function InsightsPage() { const [query,setQuery] = useState(''); const [filter,setFilter] = useState('All'); const filtered = useMemo(() => insights.filter((i) => (filter === 'All' || i.category === filter) && `${i.title} ${i.summary}`.toLowerCase().includes(query.toLowerCase())), [query,filter]); return <><Header /><main id="main-content"><Hero eyebrow="Research & insights" title="Evidence for better Basin decisions." description="A knowledge space for research, policy thinking and practical insight." /><section className="container-site py-20 md:py-28"><SectionHeading eyebrow="Knowledge library" title="Search the perspective you need." /><div className="mt-10 flex flex-col gap-3 md:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={18} /><span className="sr-only">Search insights</span><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search insights" className="w-full rounded-lg border border-slate-300 py-3 pl-10 pr-4 font-open-sans text-sm outline-none focus:border-surbach-water" /></label><select value={filter} onChange={(e)=>setFilter(e.target.value)} className="rounded-lg border border-slate-300 bg-white px-4 py-3 font-open-sans text-sm"><option>All</option><option>Research</option><option>Policy Brief</option><option>Basin Insight</option><option>Climate</option><option>Water</option><option>Public Health</option><option>Commentary</option></select></div><div className="mt-10 grid gap-5 lg:grid-cols-2">{filtered.map((insight) => <InsightCard key={insight.slug} insight={insight} />)}</div></section><CTASection dark /></main><Footer /></> }
