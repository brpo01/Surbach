import Link from 'next/link'
import { ArrowRight, CircleCheck } from 'lucide-react'
import { Header, Footer, Hero, SectionHeading, MethodologyStep, CTASection, methodology } from '@/components/site'
export const metadata = { title: 'Our Approach' }
export default function ApproachPage() { return <><Header /><main id="main-content"><Hero eyebrow="Our approach" title="From Evidence to Impact." description="SURBACH uses a disciplined five-stage methodology to help partners understand Basin conditions, make decisions and deliver resilient solutions." /><section className="container-site py-20 md:py-28"><SectionHeading eyebrow="The methodology" title="A clear path through complexity." /><div className="mt-12 flex flex-col md:flex-row">{methodology.map((step, index) => <MethodologyStep key={step.number} step={step} last={index === methodology.length - 1} />)}</div></section><section className="bg-surbach-light py-20 md:py-28"><div className="container-site grid gap-12 lg:grid-cols-[.75fr_1.25fr]"><SectionHeading eyebrow="Cross-cutting practice" title="Science + Policy + Finance + Institutions + Communities" description="Good Basin solutions are not built by one discipline alone. We connect the evidence base, the policy environment, the investment case, the institutions responsible for delivery and the communities who live with the outcome." /><div className="grid gap-4 sm:grid-cols-2">{['Science','Policy','Finance','Institutions','Communities'].map((item) => <div key={item} className="flex items-center gap-4 rounded-xl border border-slate-200 bg-white p-5"><CircleCheck className="text-surbach-green" size={20} /><span className="font-montserrat font-semibold text-surbach-navy">{item}</span></div>)}</div></div></section><section className="container-site py-20 md:py-28"><div className="grid gap-10 md:grid-cols-2">{methodology.map((step) => <article key={step.title} className="border-l-2 border-surbach-water pl-6"><span className="font-mono text-xs font-semibold tracking-widest text-surbach-water">{step.number}</span><h2 className="mt-2 font-montserrat text-2xl font-semibold text-surbach-navy">{step.title}</h2><p className="mt-3 font-open-sans leading-7 text-slate-600">{step.text} Our team brings the relevant data, disciplines, stakeholders and decision points into a shared process, producing outputs that can move into the next stage.</p></article>)}</div></section><CTASection /></main><Footer /></> }
